apiVersion: chaos-mesh.org/v1alpha1
kind: TimeChaos
metadata:
  name: sia-consumer-clock-skew
  namespace: chaos-testing
spec:
  mode: all
  selector:
    namespaces: [sia]
    labelSelectors:
      app.kubernetes.io/component: consumer
  timeOffset: "+90s"
  duration: 5m
