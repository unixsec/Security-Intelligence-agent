# SIA Local Development Dependencies (Lightweight)
# Usage: pip install -r requirements-dev-local.txt
# Skips: sentence-transformers (PyTorch ~2GB), crawl4ai (Playwright),
#         pymilvus (no Milvus), weasyprint (PDF), opentelemetry (tracing)

# --- Core Web Framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0

# --- Database ---
sqlalchemy[asyncio]>=2.0.30
aiomysql>=0.2.0
alembic>=1.13.0

# --- Redis ---
redis[hiredis]>=5.0.0

# --- HTTP Client ---
httpx>=0.27.0

# --- LLM SDKs ---
anthropic>=0.39.0
openai>=1.50.0          # Also needed for local_openai_compat provider init

# --- RSS Parsing ---
feedparser>=6.0.0

# --- Templates & Config ---
jinja2>=3.1.0
pyyaml>=6.0

# --- Scheduling ---
apscheduler>=3.10.0

# --- Auth ---
passlib[bcrypt]>=1.7.4

# --- NLP (lightweight) ---
langdetect>=1.0.9

# --- File Watching ---
watchdog>=4.0.0

# --- Test ---
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
pytest-mock>=3.14.0
respx>=0.21.0

# --- Dev ---
ruff>=0.7.0
